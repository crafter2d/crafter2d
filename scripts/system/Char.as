
package system;

class Char
{
	public native static boolean isWhitespace(char c);
	public native static boolean isAlphaNum(char c);
	public native static boolean isAlpha(char c);
	public native static boolean isDigit(char c);
}
