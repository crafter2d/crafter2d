
#include "particle.h"

#include "core/defines.h"

namespace Graphics
{
   Particle::Particle():
      pos(),
      vel(),
      color(),
      size(0),
      activeTime(0),
      lifeTime(0)
   {
   }
}
